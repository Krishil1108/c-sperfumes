rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to products, siteSettings, and orders collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
